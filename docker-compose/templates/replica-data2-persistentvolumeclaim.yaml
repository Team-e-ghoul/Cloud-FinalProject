apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: replica-data2
  name: replica-data2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
