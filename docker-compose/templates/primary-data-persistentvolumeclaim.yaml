apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: primary-data
  name: primary-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
